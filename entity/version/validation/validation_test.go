package validation

import (
	"testing"
)

func TestFormat(t *testing.T) {
	/*tests := []struct {
		version string
		isValid bool
	}{
		{"v1.0.0", true},
		{"v1.0", true},
		{"v1", true},
		{"v1.0.0-beta", false},
		{"1.0.0", false},
		{"v1.0.0.0", false},
	}

	for _, test := range tests {
		t.Run(test.version, func(t *testing.T) {
			validated := Format(test.version)
			if test.isValid {
				if validated != test.isValid {
					t.Errorf("Format(%s) = %s; expected %s", test.version, validated)
				}
			}
		})
	}*/
}
